---
const {
  source = 'newsletter',
  layoutClass = 'space-y-3',
  inputClass = '',
  buttonClass = '',
  buttonText = 'Subscribe',
  inputPlaceholder = 'Enter your email',
  redirect = '',
} = Astro.props;

const endpoint = '/api/subscribe';
const fieldId = `email-${String(source).replace(/[^a-zA-Z0-9_-]/g, '-') || 'newsletter'}`;
const disableSubmit = !import.meta.env.BUTTONDOWN_API_KEY;
---

<form action={endpoint} method="POST" class={layoutClass} data-newsletter-form>
  <input type="hidden" name="source" value={source} />
  {redirect && <input type="hidden" name="redirect" value={redirect} />}
  <input type="text" name="botcheck" class="hidden" tabindex="-1" autocomplete="off" />

  <label for={fieldId} class="sr-only">Email address</label>
  <input
    id={fieldId}
    name="email"
    type="email"
    placeholder={inputPlaceholder}
    class={inputClass}
    required
  />

  <button type="submit" class={buttonClass} disabled={disableSubmit}>
    {buttonText}
  </button>

  {disableSubmit && (
    <p class="text-xs text-amber-700 mt-2">
      Add a BUTTONDOWN_API_KEY environment variable to enable live signups.
    </p>
  )}
</form>
